# 部署报告 v0.33.0 — 正式发布（bazi-paipan · GitHub Pages）

> 版本：v0.32.0 → **v0.33.0**（MINOR +1，向后兼容的新功能：档案列表按名字拼音排序）
> 发布类型：**正式发布**（commit + annotated tag + push；线上 GitHub Pages）
> 发布日期：2026-09-12 ｜ 线上复验日期：2026-09-12（发版当晚）
> 真相源仓库：`/Users/feng/.clacky/ext/local/bazi-paipan`
> 线上入口：https://bangshun2025.github.io/bazi-paipan/
> 回滚基线：`a672efe`（tag `v0.32.0`）

## 1. 版本号

- 旧版本：v0.32.0
- 新版本：**v0.33.0**
- SemVer 判定：MINOR +1（新增排序功能，向后兼容；`ext.yml` version `"0.32.0"` → `"0.33.0"`）
- 用户授权：邦顺当轮指令「现在发版上线：提交 + 打 v0.33.0 标签 + 推送 + 线上逐字节对账」

## 2. 提交与标签

| 项 | 内容 |
|---|---|
| release commit | `e7304dcf1bd986bd38720a219b0cf25a1e2316ab`（`release: v0.33.0 档案列表按名字拼音排序`） |
| 提交时间 | 2026-09-12 13:46:37 +0800 |
| 变更规模 | 7 files changed, **+408 / −23** |
| 文件清单 | `archive.js` / `index.html` / `standalone.html` / `standalone-split.html` / `CHANGELOG.md` / `ext.yml` / `docs/实施记录-v0.33.0-档案拼音排序.md` |
| 标签 | annotated tag `v0.33.0`（tagger: bangshun，2026-09-12 13:46:46 +0800） |
| 推送后状态 | `## main...origin/main`（干净，无未推送提交） |
| pre-commit 钩子 | 未跳过（ext verify + `scripts/check-release.sh` 全绿后放行） |

## 3. 线上产物逐字节对账

对账方法：`curl --compressed` 自线上取字节 → `md5` + `wc -c` → 与本地同文件 `diff -q` 比对。

| 资源 | 本地 md5 | 线上 md5 | 字节 | 逐字节一致 |
|---|---|---|---|---|
| `index.html`（线上真相源） | `fa4627f3b4975a1dff549c733f9373a2` | 同左 | 1,170,424 | ✅ |
| `standalone.html` | `fa4627f3b4975a1dff549c733f9373a2` | 同左 | 1,170,424 | ✅ |
| `standalone-split.html` | `9c7f11230804b34495ed62f5800a9aef` | 同左 | 77,163 | ✅ |
| `archive.js` | `73edeeda132ca954b820c627c47766e6` | 同左 | 57,425 | ✅ |

- 线上 HTTP 状态：4 项均 `200`
- 线上版本头：`<!-- 八字排盘 · 从真版 v0.33.0 | 2026-09-12 -->` ✅
- `index.html` ≡ `standalone.html`（本地与线上同为同一 md5）

## 4. 线上回归（`?test=1`）

| 项 | 结果 |
|---|---|
| 断言总数 | 714 |
| 失败 | **2 条，均为 v0.30 T12 历史遗留**（非本次回归） |
| 遗留明细 | ① `v0.30 T12: 空查询 60 行态免纵向滚动 scrollHeight-clientHeight ≤2px 期望 "true" 实际 "false"` ② `v0.30 T12: .xy-settings-body 溢出像素 11` |
| v0.33 新增 12 条 | **全部通过**（T01–T06：拼音序 vs 码位序 / 隐私两档排序键 / 落盘序=显示序 / 索引定位 / 同名与幂等 / 脏数据兜底） |
| v0.30 之前各版断言 | 无新增失败 |

## 5. 线上真实数据实测（87 条档案，只读核验）

方法：真实浏览器打开线上站点 → 档案面板切「全部」→ 脚本只读比对 DOM 与 `localStorage`（仅输出计数/布尔，不记录任何档案姓名）。

| 核验项 | 结果 |
|---|---|
| 档案条数（`bz_archives_v2`） | 87 ｜ 面板渲染 87 行 |
| DOM 行序 = 落盘序 | **87/87** 行显示名与落盘顺序逐字一致 |
| 相邻显示名拼音升序（`localeCompare('zh')`） | `badPairs: 0` |
| 行 i → `openEditPanel(i)` 索引映射 | `true`（每行按钮索引 = 其 DOM 位次） |
| 真实鼠标点击第 5 行「修改」 | 编辑面板载入的姓名/小名 === 落盘第 5 条（逐字比对 true/true），索引寻址端到端正确 |

隐私说明：本报告随仓库公开（Pages 服务仓库根），故不记录任何真实档案姓名，仅留聚合计数与布尔结论。

## 6. 发布前校验（本地）

| 项目 | 结果 |
|---|---|
| `node --check archive.js` | 通过 |
| `scripts/sync-module-inline.py`（校验模式） | 五个核心模块三端一致；`archive` 外部 ≡ 两处内联（md5 `22a7c03d`，57,382 B） |
| `scripts/check-release.sh`（6 步） | 🎉 全绿（含节气真值门禁、KEYS 三端一致） |
| 本地 `?test=1` | 713 条断言全绿；v0.33 新增 12 条全过 |
| 本地实机（本地 HTTP + 真实浏览器，18 条真实形态档案） | 渲染顺序 = 拼音序；落盘 id 序列 = 显示顺序；真实鼠标点第 5 行「排盘」→ 正确加载对应档案（2019-08-23 04:00） |
| 上线前比较器（真实 87 条数据） | `orderedFully: true`、`movedRows: 84`、`dupKeys: 6` |

## 7. 遗留与备注

- **v0.30 T12 两条红断言**：线上稳定复现（本地某窗口尺寸下不复现），与窗口尺寸相关。长期为红会掩盖真问题，建议收掉（修布局或改为不依赖窗口尺寸的断言）。
- **无一次性补丁脚本**：改动直接落在 `archive.js` 真相源，经 `sync-module-inline.py --from-external archive --apply` 反向回推两处内联段，幂等可重复。
- **回滚方式**：代码回滚 `git revert e7304dc`（或回到 `a672efe`）；线上回滚需重新 push 触发 Pages 部署。tag `v0.33.0` 保留作为发布点。
- **文档配套**：`docs/实施记录-v0.33.0-档案拼音排序.md`（实现与约束）+ `CHANGELOG.md` v0.33.0 条目 + 本报告（线上证据）。
- 排序键设计取舍（隐私开关联动、拉丁名排在汉字之后）见实施记录「后续可选」一节。
