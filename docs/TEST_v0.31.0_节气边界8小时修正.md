# TEST · v0.31.0 节气边界 8 小时修正

- 版本：v0.31.0（2026-09-12）
- 范围：`?test=1` 内建断言（T05–T11）、`scripts/check-term-truth.js`、`scripts/sync-module-inline.py`、`scripts/check-release.sh`
- 断言语义基准：`switchMs = 覆盖节气时刻的那一分钟`（04:01:51 → 04:02 换月；09:05:35 → 09:06 换月）。真值来源：香港天文台（HKO）公开节气时刻 + JPL DE421 独立计算结果，两者互校

---

## 1. T05–T11 断言（`main.js` → `testTermBoundaryTruth`）

夹具集中在 `testTermBoundaryTruth()` 内（含真值来源注记），本文件只记语义与期望：

| 断言 | 语义 | 期望 |
|---|---|---|
| T05 | 2026 立春前 1 分钟（04:01） | 月柱「丑」（未换月） |
| T06 | 2026 立春分钟（04:02） | 月柱「寅」（已换月） |
| T07 | 2026 立春分钟（04:02）年柱 | 「丙午」；前 1 分钟（04:01）为「乙巳」 |
| T08 | 1987 立夏前 1 分钟（09:05） | 月柱「辰」 |
| T09 | 1987 立夏分钟（09:06） | 月柱「巳」 |
| T10 | 节气名 与 换月 同源 | 09:05 显示「清明」（表内位置：立夏前一节）；09:06 显示「立夏」 |
| T11 | 起运回归（1988 惊蛰后出生） | 起运年数 ∈ [5, 20]（修复前为 121 年 8 月级） |

要点：T05–T09 的两点必须**同时跨过真值边界**；v0.23.4 的 T01/T02（06:30 / 18:00）两点位于真值与错误边界同侧，故对 8h 缺陷不敏感 —— 这是缺陷得以滞留的原因，新增断言刻意取「分钟级夹逼」。

## 2. `scripts/check-term-truth.js`（无浏览器门禁）

- 加载方式：Node `vm` 加载 `constants.js` + `algorithm.js`，不依赖 DOM
- 内容：
  1. 显示层真值 4 条（与独立源比对，容差 ±60s）
  2. 通用边界扫描：12 节气 × {1987, 2026} × 3 采样点（节气前 1 分钟 / 节气分钟 / 节气后 1 分钟），逐点比对换月结果与 `getSolarTerm` 真值的一致性
  3. 锚点断言 10 条（与 T05–T11 同源真值，独立实现）
- 反向验证（门禁方向正确性证明）：以 v0.30.0 代码（`git show v0.30.0:algorithm.js`）替换后运行 → **30 项失败（24 个换月边界点 + 6 条锚点）**，显示层 4 条仍全绿；修复后 100% 通过

## 3. `scripts/sync-module-inline.py`（三端一致性门禁）

- 切段规则：以「下一个模块标记」为边界（`constants` / `algorithm` / `archive` / `render` / `main` 五个大模块共用同一 `<script>` 块，按 `<script>` 边界切会误判）
- 比对：外部 `*.js` 主体（剥离头部注释块）≡ `index.html` 内联段 ≡ `standalone.html` 内联段
- 默认只检查（退出码 1 = 有漂移）；`--apply` 才写回，`--set-version X.Y.Z` 同时更新头部版本
- 反向验证：v0.30.0 状态运行 → 检出 `algorithm.js` / `archive.js` 两项漂移；重写后五模块全绿

## 4. `scripts/check-release.sh`（6 步）

1. 版本号一致性 2. `index.html` ≡ `standalone.html`（整文件） 3. `node --check` 三文件 4. 七个小模块内联一致性 5. `sync-module-inline.py` 6. `check-term-truth.js`

以仓库根运行：`bash scripts/check-release.sh .` → 6/6 全绿，退出码 0。

## 5. 浏览器端实测（发布前）

| 环境 | 命令/URL | 结果 |
|---|---|---|
| `standalone.html`（修后） | `?test=1` | 674 条断言通过（绿态计数 673：v0.30 T12 的条件几何断言仅在溢出时入列） |
| `index.html`（修后） | `?test=1` | 全绿 |
| 反向对照（v0.30.0 站点） | `?test=1` | 664/664 全绿 —— 说明既有断言确实拦不住该缺陷 |

## 6. 观察项（非本次范围）

- **v0.30 T12 几何断言（既有，非本版引入）**：「设置页 60 行态免纵向滚动 `scrollHeight - clientHeight ≤ 2`」依赖视口高度 —— 1512×771 下稳定溢出 11px（`sh 582 / ch 571`）；**以 v0.30.0 原始字节运行同样失败**（即非本版回归）。CI 用 Puppeteer 默认 800×600 视口，`wide()`（`min-width:1280px`）为假 → 该分支不执行，部署门禁不受影响。后续可单独排期（对齐浮层高度与视口高度，或按「无内容裁切」而非「≤2px」判据）

## 7. 未覆盖

- 1900 年前节气表精度、表范围外年份（1000 前 / 2100 后）行为：沿用既有约定（`getSolarTerm` 返回 `null` + 既有 null 防护）
- 真太阳时均时差（Spencer 公式）秒级精度：与本次缺陷无关，未纳入本版门禁
