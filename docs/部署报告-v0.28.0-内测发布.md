# 部署报告 · v0.28.0 内测发布

> 版本：v0.27.0 → **v0.28.0**（MINOR+1，向后兼容的功能口径变更）
> 发布类型：**内测版**（local 层，不 push、不 git commit）
> 发布内容：当年节气数据区 · 每列最后一行「干支」由**交节日柱**改为**该节月建干支**（五虎遁直推）
> 发布师：worker_e14490c4 ｜ 编排：orch_eccabe4429eb
> 日期：2026-09-10 ｜ 发布时刻：23:04
> 容器：`/Users/feng/.clacky/ext/local/bazi-paipan`
> 回滚基线：tag `v0.26.0`（或回退 ext.yml 至 0.27.0）

---

## 一、版本号

| 项 | 内容 |
|---|---|
| 旧版本 | v0.27.0 |
| 新版本 | **v0.28.0** |
| 递增类型 | **MINOR +1**（SemVer 2.0.0：向后兼容的新功能 / 口径变更） |
| `ext.yml` version | `"0.27.0"` → `"0.28.0"`（**纯数字三段**；verify schema 不接受 `v` 前缀） |
| 上位版本 tag | `v0.26.0`（已正式发布） |
| 承接 | v0.27.0 内测态（节气区真太阳日月/时间）之上叠加；本版**仅换干支数据源、版式零变化** |

---

## 二、变更清单

### Changed（功能由编程师交付并 freeze，发布师不改功能代码）
- **节气区干支数据源**：12 节气列最后一行「干支」由**交节日柱**（旧口径，如 1982 立春列显 `戊午`）
  改为**该节所起月份的月建干支（月柱）**（新口径，如 `壬寅`）；按**五虎遁**（年干推月干）直推。
  `render.js` 新增纯函数 `jieqiMonthGZ(yearGan, idx)` 并挂 `RENDER.jieqiMonthGZ`
- **年基口径**：整块 12 列统一取「所选干支年」的年干（`ALGO.yearPillar(year).gan`）为五虎遁年基；
  小寒列同取**本年**年基（`jieqiMonthGZ('癸',11)` = `乙丑`；反例：误用次年干得此值）
- **版式零变化**：仅换数据源，列数 / 行序 / CSS / DOM 结构不变；`algorithm.js`、`constants.js` **零改动**，
  排盘口径与四柱（D1）不动
- **自动化断言 393 → 429**：`?test=1` 新增 v0.28 T01–T04 共 **36** 条
- **三端内联同步**：`index.html` / `standalone.html` 内联 JS + `main.js` 断言段
- **版本号**：`ext.yml` `0.27.0` → `0.28.0`

### 发布师本次动作（仅升版 + 注释 + 文档，零功能改动）
- `ext.yml`：`version` `0.27.0` → `0.28.0`
- `index.html` / `standalone.html`：头部版本戳 → `从真版 v0.28.0 | 2026-09-10`；新增 v0.28.0 功能历史注释行；内联副本头 2 处随升
- `render.js` / `main.js`：文件头版本注释 → `v0.28.0`
- `CHANGELOG.md`：新增 `## v0.28.0` 段（Keep a Changelog：Changed / Notes）
- `SYSTEM.md`：`当前版本：v0.28.0` + 版本历史表首行插入 v0.28.0 行
- **未改**：`scripts/check-release.sh`（本版无新增 DOM id，无需扩展）、`standalone-split.html`（遵 Leader 纪律）

### 已交付文档（编排侧，主仓库 `docs/` 均已落位）
`docs/PRD_v0.28.0_干支行改月建干支.md`、`docs/ADR_v0.28.0_干支行改月建干支.md`、
`docs/report_v0.28.0_干支行改月建干支.md`、`docs/部署报告-v0.28.0-内测发布.md`（本报告）

---

## 三、md5 前后对照（freeze 基线 → 内测发布后）

| 文件 | freeze 基线 md5（编程师交付） | 发布后 md5 | 说明 |
|---|---|---|---|
| `render.js` | `93bf6cb463d1486c46b3f03696c430e0` | `f37a5f02ca1b2541b206ddc86031bf39` | 仅第 1 行版本注释 |
| `main.js` | `e53aa8854840d4250fd140ca405a200f` | `c6449c1856d8be01d0495acdf35f193a` | 仅第 1 行版本注释 |
| `index.html` | `afd2780604b12431deae325915c7df39` | `2145f7721db967d7eb7e5f2a53a7a5eb` | 仅 3 处注释行 |
| `standalone.html` | `afd2780604b12431deae325915c7df39` | `2145f7721db967d7eb7e5f2a53a7a5eb` | 同上；与 index **字节仍一致** |
| `standalone-split.html` | `238a01d00b1b08310652213d9efb197d` | `238a01d00b1b08310652213d9efb197d` | **未动**（遵纪律） |

### 「仅注释行」可逆证明（发布师改动 = 纯注释）
将发布师改动（版本戳 v0.28.0 → v0.27.0、删除新增功能注释行）逐项撤销后，md5 **精确回到 freeze 基线**：
```
render.js      reconstruct = 93bf6cb4…  == freeze   MATCH=True
main.js        reconstruct = e53aa885…  == freeze   MATCH=True
index.html     reconstruct = afd27806…  == freeze   MATCH=True
standalone.html reconstruct = afd27806…  == freeze   MATCH=True
```
`git diff`（相对 freeze 语义）逐行差异仅 4 处，全部为 HTML/JS **注释行**：
1. `<!-- 八字排盘 · 从真版 v0.27.0 | 2026-09-10 -->` → v0.28.0
2. 新增 `<!-- v0.28.0: 干支行改月建干支 … -->`
3. `/* 八字排盘 v0.27.0 — render.js */` → v0.28.0（内联副本）
4. `/* 八字排盘 v0.27.0 — main.js */` → v0.28.0（内联副本）
（`render.js` / `main.js` 外部文件各 1 处文件头注释）

---

## 四、验证结果

### 4.1 `clacky ext verify`
- 本扩展三单元 **全 [OK]**：
  `[OK] bazi-paipan/launcher`、`[OK] bazi-paipan/paipan`、`[OK] bazi-paipan (api → /api/ext/bazi-paipan/)`
- contributes 声明文件存在性：`api/handler.rb`、`panels/launcher/view.js`、`panels/paipan/view.js` 均在
- 其余容器中 3 处 `schema.invalid_version` **ERR** 属存量（见 §六 OBS-03），**非本版范围**

### 4.2 `scripts/check-release.sh`
- **EXIT = 0**，输出：`🎉 全部校验通过，可以发布。`
- 【1/4】三文件内联 JS 语法（node --check）：index / standalone / standalone-split 全 ✅
- 【2/4】index.html vs standalone.html 11 模块段一致性：全 OK（含 `render.js` / `main.js` 段一致）
- 【3/4】三文件关键 id 存在性：含 `jieqi-section` / `jq-gz` / `jq-tsmd` / `jq-tstm`（三端均在 / 运行时生成）✅
- 【4/4】外部 JS vs 单体版内联段一致性（防漂移）：全 ✅

### 4.3 `?test=1` 双环境断言（应为 429 条）
| 环境 | 入口 | 标题 | 结果 |
|---|---|---|---|
| file:// 单体版 | `file:///…/bazi-paipan/index.html?test=1` | ✅ 全部通过 — 八字排盘回归测试 | **全部 429 条断言通过！** ❌ 0 |
| http 模块版 | `http://localhost:7070/api/ext/bazi-paipan/standalone?test=1` | ✅ 全部通过 — 八字排盘回归测试 | **全部 429 条断言通过！** ❌ 0 |

> 429 = v0.26 (339) + v0.27 (54) + v0.28 (36)。新增 v0.28 T01–T04：
> 1982 十二节锚点 12/12、五虎遁纯函数含反例、经度无关性、真实渲染 + `refreshJieqi` 一致性。

### 4.4 server 热加载实测（改代码无需重启）
| 端点 | 结果 |
|---|---|
| `GET /api/ext/bazi-paipan/render.js` | HTTP 200，94968 B，`v0.28.0` 命中 3 |
| `GET /api/ext/bazi-paipan/main.js` | HTTP 200，89768 B，`v0.28.0` 命中 2 |
| `GET /api/ext/bazi-paipan/standalone` | HTTP 200，64769 B（服务 `standalone-split.html`，见 §六 OBS-04），`jq-tsmd` 命中 1 |
| `GET /api/ext/bazi-paipan/paipan?…`（API） | HTTP 200，5652 B |
> OpenClacky dispatcher 每请求按 `handler.rb` / `ext.yml` 的 mtime 指纹 `ensure_fresh` 热重载；
> 本次 `ext.yml` / `render.js` / `index.html` / `standalone.html` mtime = 2026-09-10 23:04:14，已生效。

### 4.5 版本一致性核验
- `ext.yml`：`version: "0.28.0"` ✅
- 三 HTML 头：`index.html` / `standalone.html` = `从真版 v0.28.0`；`standalone-split.html` 仍 v0.27.0（见 OBS-04）
- `render.js` / `main.js` 头 + 内联副本头：`v0.28.0` ✅
- `SYSTEM.md`：`当前版本：v0.28.0` + 历史表首行 v0.28.0 ✅
- `CHANGELOG.md` 顶部：`## v0.28.0 — 当年节气数据区 · 干支行改为月建干支 (2026-09-10)` ✅

---

## 五、如何启用（内测环境）

1. 扩展位于 local 层，server 启动即挂载，**无需安装/注册**。
2. 打开内测入口（任选）：
   - 面板：Clacky 侧栏「八字排盘」→ 排盘面板
   - 模块版独立页：`http://localhost:7070/api/ext/bazi-paipan/standalone`
   - 回归自测：`…/standalone?test=1`，或 `file://…/bazi-paipan/index.html?test=1`（单体版）
3. 排盘后查看「大运·流年」区下方节气块：每列最后一行「干支」现为**该节月建干支**
   （如 1982 立春列 = `壬寅`，旧口径为 `戊午`）。
4. 改代码后无需重启 server（mtime 指纹热重载，见 §4.4）。

---

## 六、遗留事项 / 观察项（均非本版缺陷）

| 编号 | 内容 | 处置 |
|---|---|---|
| OBS-01 | `check-release.sh` 第 4 段（外部 vs 内联一致性）不含 `render` / `main`，存在盲区 | 已用**人工 diff 兜底**（§三 证明 + 【2/4】模块段一致性覆盖）；建议后续补入第 4 段 |
| OBS-02 | PRD §4.3 示例数值勘误「甲寅」应为「乙丑」 | 文档勘误，**不影响功能**（ADR/断言取值为 `乙丑`）；建议 PM 后续修订 |
| OBS-03 | `clacky ext verify` 对 `bazi-paipan-test` / `qimen-paipan` / `qimen-paipan-test` 报 `schema.invalid_version`（`v` 前缀 ext.yml） | **存量**、非本版范围；建议另行安排清理为纯数字三段 |
| **OBS-04（新增）** | `/standalone` 端点实际服务 **`standalone-split.html`**（模块版）；遵 Leader「不动 standalone-split.html」纪律，其头部注释戳仍为 `从真版 v0.27.0`，与其余端 v0.28.0 不同步（**仅源码注释，用户不可见**） | 提请注意，**是否补 1 行注释**请 Leader 裁决 |

---

## 七、纪律声明

- **未改功能代码**：仅升版 + 注释 + 文档；已用 md5 可逆证明（§三）确认改动全部为注释行。
- **未 git commit、未 push**：`git HEAD` 仍为 `28940ad`（v0.26.0 正式部署报告）。
- **未动 `standalone-split.html`**。
- 待用户 L4 手动验收确认后再走正式发布（届时统一 commit + 打 `v0.28.0` tag）。

---

*本报告双副本：发布师工作目录 + 主仓库 `docs/`（md5 一致）。*
