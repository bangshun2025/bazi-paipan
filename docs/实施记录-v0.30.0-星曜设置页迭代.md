# 实施记录 · v0.30.0 星曜设置页迭代（搜索筛选 / 三栏同屏 / 纳音全名）

- 版本：**v0.30.0**（`ext.yml` 仍 0.29.0，由发布师在发布环节升版）
- 权威口径：**PRD r7** `2c82f007f4983ce031c8447b8b433d7a`（566 行，AC01–AC25，唯一源；r5/r6 已作废）／**ADR v1.6** `db2d693ccf24581b426b46875d27a714`（826 行 / 68606 B，双副本一致；v1.5 `89a84227…` 因**编号勘误**（§15.3 归属条、§15.8 第 2 点误用 `R7` → 已改 `R8（原称 R7）`）被取代，v1.4 `5e4c1776…` 更早作废）
- C3（共享表头）补充权威口径：架构师**冻结期纪事**（文件名 **`_RECORD_ADRv1.4冻结期_C3最终recipe与留痕_20260911.md`**，与 ADR v1.6 §13 引用**逐字一致**；双副本 = 真相源 `bazi-paipan/docs/` + 架构师工作目录 `架构师/docs/`，md5 一致；内测容器不含、编程师工作目录不含；**不另建短名副本以免双真相源漂移**）—— **§3 / §7 = C3 权威口径**、§8.4 = v1.5 执行记录、§8.5 / §8.7 = R 编号（正式 `R8（原称 R7）`）；当时快照 md5 `733e940b1f7f7a78a4f596ef82cb7509`（204 行 / 20049 B）—— **纪事 md5 不入正文（避免自指失真），仍会滚动，引用请以「文件名 + §号」为主、md5 只作快照**；测试侧规格 `docs/布局断言规格草案-v0.30.0.md` v0.7 `1701a66bb73c88c78c044d3b970133f3`、`docs/测试执行说明-v0.30.0.md` v1.2 `ebfa06febfc46b0ddf2c3e779f4ebecc`
- ADR 勘误与本实现的关系：**v1.5** 改正文 3 处（§15.3 C3 的 `gap:0`→`gap:4px`、删「<1280px 补 `thead{width:100%}`」、§15.7 ⑤ 同一过期指令）＋ **v1.6** 改编号 2 处（`R7`→`R8（原称 R7）`）+ §13 一行 + 版本行 ⇒ **代码/产物零改动**；`index.html:286–288`（base `display:block` + 4 轨道同列模板）+ `:302`（媒体内仅 `width:calc((100% - 32px)/3)`）**正是勘误后的口径**；6 处代码注释（三端 `:301`、`main.js:2248` C8 注释等）按「作用域准确的历史引用」保留，不改
- 实施者：编程师 `worker_9128f819`；实施日期：2026-09-11
- 真相源仓库：`/Users/feng/.clacky/ext/local/bazi-paipan`；内测容器副本：`/Users/feng/.clacky/ext/local/bazi-paipan-test`
- 迭代前快照：`.bak_v0.30.pre/`（12 文件）

---

## 1. 改动清单与 md5（改前 → 改后）

| 文件 | 改前 md5（迭代前快照） | 改后 md5（本记录冻结值） | 改动摘要 |
|---|---|---|---|
| `xingyao.js` | `a16caa34be449536f91f459a3d224f7d` | **`f07f04ece72e0ae243010055f46a485d`** | 5 处：移 `_groups` 行缓存；`buildRowCache()` 去组标题行查询；`renderSettings()` 不再生成组标题行（只 60 数据行，保留 `data-xy-group`）；`resetSearchUI()` 删组标题复位；`filterRows()` 删 `gvis` 与空组隐藏循环 |
| `main.js` | `87539bce4181ea23ff9e79cf926eff7e` | **`1d43775820e630fa0dbfbb329e74416a`** | 断言段重写：T02/T03/T05/T06/T09 改静态栏头口径；新增 T12（免滚动）/T13（三栏几何）/T14（空栏占位+空提示唯一）/T15（AC25 栏位正确性）/ **C8（表头几何 3 条）**；新增 helper `headsAll/wide/visCountOf/headsOk/rowsTdOk/headRects/bandBad`；测试态环境归一 IIFE |
| `index.html` | `26cc3e70f46412e8392487c6369871b8` | **`eb27d3a5b25bb2377dd228e4c207b2e6`** | 弹层尺寸 `90vw/1200px/92vh`；`.xy-table` 三栏 CSS 块；静态栏头 markup；`thead` 共享表头 CSS；行内输入框与搜索栏 padding；`tr.xy-group-row` 旧规则整块替换 |
| `standalone.html` | `26cc3e70f46412e8392487c6369871b8` | **`eb27d3a5b25bb2377dd228e4c207b2e6`** | 同上（与 `index.html` **字节一致**，`cmp` 已验） |
| `standalone-split.html` | `d4640340ef0f8ad2835fcba827386ee9` | **`4cbcb3affe9f0de78a0dbf36cdaabb19`** | 同 `index.html`（模块版，不外联内联段） |
| `scripts/check-release.sh` | `78984aeba6ec3d5436d06369d950bca7` | **`a4241cb0af78cf99e7a74b49cf1f0486`** | `KEYS` 追加 `xySearchBar/xySearchInput/xySearchClear/xySearchCount/xySearchEmpty/xy-group-title`；`RUNTIME_KEYS` 改为 `jieqi-section jq-gz jq-tsmd jq-tstm xy-trigger`（移除 `xy-group-row`）；【3/4】兜底扫描含 `xingyao.js` |
| `docs/ALGORITHM.md` | — | 未变（`78b6eb93c159…`） | §21.2 表头「纳音」+ 完整纳音名（v0.30 前半程已随 `sync-xingyao-algorithm.py` 同步，本阶段复跑幂等无变更） |
| `CHANGELOG.md` | `470e142fe67abc88be64f7410e8ac3c9`（v0.26 基线） | **`cc23460b3f0daadf915e24e8dc96b76e`** | v0.30.0 段按 r7 口径重写（三栏同屏 / 静态栏头 / C8 / 免滚动 / 已知限制） |

中间态（供追溯）：`main.js` 曾为 `0450ecf673f5a49a0602e9d24afc2fe3`（补 C8 前）、`index.html`/`standalone.html` 曾为 `634c7e9513543320b326331911bde35f`（td padding 收紧前）。

---

## 2. 关键实现

### 2.1 三栏同屏 CSS（最终定稿，`index.html` 约 283–310 行）

```css
/* mobile-first：<1280px 单栏纵向（FR3.7/E15，已知行为 L，允许纵向滚动） */
.xy-groups-heads { display:grid; grid-template-columns:minmax(0,1fr); }
.xy-group-title { font-family:var(--font-display); font-size:13px; letter-spacing:.05em;
  color:var(--c-ink); padding:4px 4px; border-bottom:1px solid var(--c-line); }
.xy-table { display:block; }
.xy-table thead { display:block; width:100%; }
.xy-table thead tr { display:grid; grid-template-columns:56px 76px minmax(0,1fr) minmax(0,1fr); gap:4px; }
.xy-table thead th { display:block; border-bottom:1px solid var(--c-line); padding:4px 6px; text-align:left; }
.xy-table tbody { display:grid; grid-auto-flow:row; grid-template-columns:minmax(0,1fr); column-gap:16px; }
.xy-table tbody > tr { display:grid; align-items:center; gap:4px;
  grid-template-columns:56px 76px minmax(0,1fr) minmax(0,1fr); }
.xy-table tbody > tr > td { display:block; min-width:0; padding:1px 6px; text-align:left;
  border-bottom:1px solid var(--c-line); }
@media (min-width:1280px) {
  .xy-groups-heads { grid-template-columns:repeat(3,minmax(0,1fr)); column-gap:16px; }
  /* ADR v1.4 §15.3 C3：表头只占一栏宽，列模板与数据行一致（标签与第 1 栏各列对齐） */
  .xy-table thead { width:calc((100% - 32px)/3); }
  .xy-table tbody { grid-auto-flow:row dense;
    grid-template-columns:repeat(3,minmax(0,1fr)); grid-template-rows:repeat(20,auto); }
  .xy-table tbody > tr[data-xy-group="1"] { grid-column:1; }
  .xy-table tbody > tr[data-xy-group="2"] { grid-column:2; }
  .xy-table tbody > tr[data-xy-group="3"] { grid-column:3; }
}
.xy-table .xy-nayin { white-space:nowrap; }
.xy-settings-modal [hidden] { display:none !important; }   /* C2 必需项：tr{display:grid} 压过 UA 的 [hidden] */
```

### 2.2 静态栏头 markup（表格上方，EN DASH U+2013）

```html
<div class="xy-groups-heads">
  <div class="xy-group-title" data-xy-group="1">第 1 组 · 甲子 – 癸未</div>
  <div class="xy-group-title" data-xy-group="2">第 2 组 · 甲申 – 癸卯</div>
  <div class="xy-group-title" data-xy-group="3">第 3 组 · 甲辰 – 癸亥</div>
</div>
```

不在 tbody、无 `data-gz`；空栏保留占位、栏头不隐藏。

### 2.3 main.js 断言（`// ===== 星曜 v0.30.0（T01–T15 + C8；ADR §8.2/§8.3、PRD r7）=====`）

- 新增 helper：`headsAll()` / `wide()` / `visCountOf(n)` / `headsOk()` / `rowsTdOk()` / `headRects()` / `bandBad()`（AC25 判据：可见行矩形中心落在其 `data-xy-group` 栏头水平区间 ±2px）
- **T02**：栏头=3、`data-xy-group`=1,2,3、栏头文案 === `X.groupTitle(n)`、栏头不在 tbody、栏头无 `data-gz`、`#xySettingsList` 仍为 TBODY、`table.xy-table` 仅 1、`.xy-table tbody` 仅 1、每行 4 td、20/20/20、组首末、DOM 序 60 行 === `X.gz60()`、**tbody 内无 `tr.xy-group-row`（不留幽灵）**
- **T03**：`.xy-table thead` 仅 1 处、`thead` computed position ≠ sticky、每行 4 td；表头[1]=「纳音」、60 行纳音 === `NAYIN[gz]`
- **T12**（免滚动 AC20）：前置 `openSettings()`+`.show`+`document.fonts.status==='loaded'`；wide 时断 `scrollHeight-clientHeight ≤2px` 且加「非退化」守卫（scroll/client 均 >0，拦 0≤0 假绿）；窄屏走 L 分支
- **T13**（三栏几何 AC21/AC22）：tbody/栏头 display≠none；栏头 top 极差 ≤8px、left 严格递增、栏宽 >0；弹层宽 ≈ `min(90vw,1200px)` ±8px
- **T14**（AC24）：癸亥→可见 1、属组 3、空栏 0/0、栏头仍 3 可见、筛选后栏头位置漂移 ≤0.5px；XYZ→`#xySearchEmpty` 全局唯一可见、可见行 0、栏头仍 3；己→2/2/2 + 可见 6
- **T15**（AC25，仅 wide）：`bandBad()` 对 己/戌/癸亥/甲子 全为空；己 命中行 x 带去重数 === 3
- **C8**（表头几何，测试契约）：`thead th`=4；逐 i=0..3 `|th[i].left − 第1栏首条可见行第 i 个 td.left| ≤12px`；`th[i].width ≥40px`（防 `1fr` 轨道塌缩的二次假绿）；仅 ≥1280px + 60 行全显态；**只取 rect，不锁** computed `grid-auto-flow`/`grid-template-*`
- **测试态环境归一**（断言段开头 IIFE）：`?test=1` 下 `.page` 内联 `display:none`（登录门）且 `auth.js` 的 `show/hideLoginScreen` early-return ⇒ 主内容恒隐藏、几何 rect 全 0，会让免滚动断言退化为 `0≤0` 假绿、栏宽 0 假红。故仅测试域内放行 `.page`（`style.display=''`）并隐藏 `#authOverlay`，**不改产品代码**。

---

## 3. 偏差登记（含被否决方案）

| # | 项 | 权威建议 | 我的实现 | 理由与裁定 |
|---|---|---|---|---|
| D19 | 切栏方式 | `grid-auto-flow:column` + `grid-template-rows:repeat(20,auto)`（r6 初稿） | **`grid-auto-flow:row dense` + 按组钉 `grid-column`** | `column` 流在**筛选态**必塌栏：`hidden` 行 `display:none` 不是 grid item、被 auto-placement 跳过 ⇒ 可见行从栏 1 顶部重排（搜「己」6 行全挤栏 1），破坏 FR3.8 空栏占位与 AC24「三栏各 2 行」。已列入**被否决方案**（D19）；Leader 三点裁定支持、PM 独立复验、写入 r7 |
| ① | 共享表头几何 | 架构师 v1.4 初稿 `gap:0` | **`gap:4px`** + `thead{width:calc((100% - 32px)/3)}` | 与数据行逐像素同构；实测偏差 `gap:4px`=**[0,0,0,0]**、`gap:0`=4–8px；架构师已自我更正并采纳（定稿 gap:4px） |
| ② | 表头宽度作用域 | 「三行整体移入 media，<1280px 零覆盖」（Leader 初版硬约束 + 架构师定稿） | **base 保留 `thead{display:block;width:100%}` + 同列模板；仅 `width:calc((100% - 32px)/3)` 进 media** | **A/B 实测**（真实 `standalone.html`）：零覆盖（回退默认 table/thead）⇒ thead 收缩 **230px**、th 宽 **64/38/64/64**、偏差 **[0, 4, −38, −347.8]**（1024×800）＝真视觉缺陷；我的写法 ⇒ 偏差 **[0,0,0,0]**、th 宽全 ≥56px、弹层内横向溢出 0。**Leader 裁定认可偏离**（架构师自查项被数据推翻，已记入其冻结期纪事 §7；架构师独立复现一致：754 vs 272 宽、[0,0,0,0] vs [0,0,0,−241]） |
| ③ | 幽灵行清理 | 旧口径保留 `xy-group-row` 组标题行 | **移除** `_groups` 行缓存、`renderSettings` 组标题行、`resetSearchUI` 复位循环、`filterRows` 空组隐藏循环；门禁 `RUNTIME_KEYS` 移除 `xy-group-row` | r6/FR3.4 改静态栏头后组标题行不再存在，按「不留幽灵」原则同步清理（含断言 T02 断「tbody 内无 `tr.xy-group-row`」） |
| ④ | 免滚动（AC20） | ADR §15.4 行高预算 ≤26px | `td` 纵向 padding `2px → 1px`（**选择器 `.xy-table tbody > tr > td`，仅星曜设置弹层**） | 行高 24.00 → **22.00px**（§15.4 预算内）；1280×800 表区纵向溢出 **15px → 0px**；三基线 0/0/0px。主盘 `div.luck-table` 零影响 |
| ⑤ | 弹层尺寸 | r6 初稿 90%/720px/85vh | `width:90vw; max-width:1200px; max-height:92vh` | 三栏 20 行同屏所需；实测弹层宽 = `min(90vw,1200px)`（1200 / 1152 / 1200）±8px 内 |
| ⑥ | 测试态环境 | — | 断言段头 IIFE 放行 `.page` + 隐藏 `#authOverlay` | 见 §2.3；仅测试域，产品代码零改动 |

---

## 4. 自测证据（2026-09-11，headless Chrome 153 / CDP）

### 4.1 `?test=1` 四环境（每环境 **664/664 全绿**，`bodyFail=0`）

| 环境 | 结果 |
|---|---|
| `file://…/standalone.html?test=1` | ✅ 664/664（`bodyOk=665`） |
| `file://…/index.html?test=1` | ✅ 664/664 |
| `file://…/standalone-split.html?test=1` | ✅ 664/664 |
| `http://127.0.0.1:8765/standalone-split.html?test=1` | ✅ 664/664 |

（断言总数 517 → 626 → 662 → **664**；662→664 为 C8 净增 2 条。）

### 4.2 门禁 `bash scripts/check-release.sh`

- 真相源仓库：**exit=0**，`🎉 全部校验通过`（✅ 139 项）
- 内测容器 `bazi-paipan-test`：**exit=0**，同上

### 4.3 三基线 CDP 实测（弹层打开、60 行全显态）

| 视口 | th 宽 | th[i].left − 第1栏 td[i].left | 表头宽 | `.xy-settings-body` 溢出 | 行高 | 弹层 | 搜「己」 |
|---|---|---|---|---|---|---|---|
| 1440×900（主基线） | 56 / 76 / 116.3 / 116.3 | **[0,0,0,0]** | 376.7 | **0px**（scroll 570 / client 570） | 22.00px | 1200×711 | 2/2/2 且栏位正确 |
| 1280×800 | 56 / 76 / 108.3 / 108.3 | **[0,0,0,0]** | 360.7 | **0px**（570 / 570） | 22.00px | 1152×711 | 2/2/2 且栏位正确 |
| 1600×1000 | 56 / 76 / 116.3 / 116.3 | **[0,0,0,0]** | 376.7 | **0px**（570 / 570） | 22.00px | 1200×711 | 2/2/2 且栏位正确 |

三档共同：`table`=1 / `tbody`=1(TBODY) / 行=60 / 栏头=3 / `thead`=1(th=4) / 幽灵行=0 / 分栏 20/20/20 / 栏头 top 极差 0.0px / 栏宽 >0；筛「戌」1/2/2、「癸亥」0/0/1、「甲子」1/0/0、「XYZ」0 行 + 空提示全局唯一 + 栏头仍 3。

### 4.4 C8 逐项取证（真实 `standalone.html`，环境归一后的可见态）

| 视口 | `th[left, width]` | Δleft（`th[i]` − 第 1 栏首行 `td[i]`） | maxΔ | minWidth | thead 宽 / 数据行宽 | 弹层内横向溢出 |
|---|---|---|---|---|---|---|
| 1440×900 | [139,56] [199,76] [279,116.3] [399.3,116.3] | **[0,0,0,0]** | 0.0 | **56.0** | 376.7 / 376.7 | 0 |
| 1280×800 | [83,56] [143,76] [223,108.3] [335.3,108.3] | **[0,0,0,0]** | 0.0 | **56.0** | 360.7 / 360.7 | 0 |
| 880×682（窄屏 N6） | [63,56] [123,76] [203,305] [512,305] | **[0,0,0,0]** | 0.0 | **56.0** | 754.0 / 754.0 | 0 |

三判据（`th`=4 / 逐列 `|Δleft|≤12px` / `th[i].width ≥40px`）在三档全部满足；`td[left,width]` 与 `th[left,width]` 逐项完全相同。窄屏 thead 宽 = 数据行宽 = 754.0（与架构师独立复现值一致）。

### 4.5 窄屏档（单栏纵向）

| 视口 | thead 宽 | th 宽 | 偏差 | 行 left | 弹层内横向溢出 |
|---|---|---|---|---|---|
| **1024×768**（架构师指定抽测） | 883.6 = 数据行宽 | 56/76/369.8/369.8 | **[0,0,0,0]** | 唯一 70 | **0** |
| 1024×800 | 883.6 | 56/76/369.8/369.8 | [0,0,0,0] | 唯一 70 | 0 |
| 768×900 | 653.2 | 56/76/254.6/254.6 | [0,0,0,0] | 唯一 57 | 0 |
| 390×844 | 313.0 | 56/76/84.5/84.5 | [0,0,0,0] | 唯一 39 | 0 |

窄屏均：单栏（行 left 唯一）、栏头 3 个纵向堆叠（top 递增）、表头与数据行对齐、**th 宽全 ≥40px 不塌缩**。

### 4.6 偏离 A/B 数据（C3 表头作用域，同份已发测试师）

| 视口 | 变体 | thead computed display | thead 宽 | th 宽 | 偏差 |
|---|---|---|---|---|---|
| 1024×800 | **A＝现写法** | block | 883.6 | 56/76/369.8/369.8 | **[0,0,0,0]** ✅ |
| 1024×800 | B＝零覆盖（默认 table/thead） | table-header-group | **230.0** | 64/**38**/64/64 | [0, 4, **−38**, **−347.8**] ❌ |
| 390×844 | A | block | 313.0 | 56/76/84.5/84.5 | [0,0,0,0] ✅ |
| 390×844 | B | table-header-group | 230.0 | 64/38/64/64 | [0, 4, −38, −62.5] ❌ |

架构师独立复现（真实 `index.html`，880×682）：现写法 thead 宽 754 = 数据行宽、delta [0,0,0,0]；强制 `display:table-header-group !important` ⇒ 收缩 272、delta [0,0,0,−241]。

### 4.7 证据文件

- 截图：`evidence_v0.30.0_r7_1440x900.png`、`evidence_v0.30.0_r7_1440x900_fullpage.png`
- 实测 JSON／日志：`/tmp/cdp_v30r7_result.json`、`/tmp/cdp_v30r7_out3.txt`（三基线 + 四环境 + 截图）、`/tmp/cdp_narrow.py`（窄屏档）、`/tmp/cdp_ab_thehead.py`（A/B）、`/tmp/cdp_overflow_src.py`（溢出源）
- 门禁日志：`/tmp/cr_src.txt`、`/tmp/cr_test.txt`

---

## 5. 遗留项 / 已知限制

1. **页面级横向溢出**（1280×800 = 15px / 1024×800 与 1024×768 = 56px / 390×844 = 276px）来自主盘 `div.luck-table`（816px 固定宽 / `min-width:620px`），**既有行为、非本版引入**，本轮不修；星曜设置弹层内横向溢出 **0**（溢出元素枚举：`inXYModal=0`）。
2. `<1280px` 单栏纵向需纵向滚动（E15，已知行为 L；AC20 免滚动仅约束 ≥1280px）。
3. 导入 JSON 后设置页列表不即时重绘（v0.29 遗留，非缺陷，Leader 裁定留 v0.30.x）。
4. `thead` 通栏图例 → 已改为「对齐第 1 栏」，若后续新增列需同步 `thead tr` 列模板与 `.xy-groups-heads` gap 常数（32px = 16px × 2 栏间距）。

---

## 6. 三条红线确认（未触碰）

1. 筛选**只改 `hidden`**，绝不删 DOM / 不重渲染，`collectDraft()` 全量遍历零改动 ✅
2. 主盘纳音行与排盘结果未动（`td padding` 改动选择器限 `.xy-table`，仅星曜设置弹层）✅
3. localStorage 键 `bz_xingyao_map` / `schemaVersion=1` 不变、不迁移、搜索不落盘 ✅

**CSS 作用域审计（规则级，供验收复核项引用）**：以迭代前快照 `.bak_v0.30.pre/standalone.html` 为对照，对 `<style>` 去注释后按「选择器 → 声明体」逐条解析比对：
- 规则数：旧 **368** → 新 **384**（+16）
- **仅新版新增 16 条**：15 条选择器名含 `.xy-*`（`.xy-search*` / `.xy-group-title` / `.xy-groups-heads` / `.xy-table thead|thead tr|thead th|tbody|tbody > tr|tbody > tr > td` / `.xy-table .xy-nayin` / `.xy-settings-modal [hidden]`），第 16 条是新增的 `@media (min-width:1280px)` 块（旧版 0 处），其块内 7 条成员**全部**为 `.xy-*`（见 `index.html:299–308`）
- **同选择器但声明体变化 4 条**：`.xy-settings-modal`（弹层尺寸）、`.xy-table`、`.xy-table th`、`.xy-table input.xy-name, .xy-table input.xy-system` —— 全部 `.xy-*`
- **仅旧版有 = 0 条**；**非 `.xy-*` 的改动规则 = 0 条**（除上述 `@media` 容器本身）
- 全局裸选择器核查：全仓**不存在** `td {` / `th {` 形式的裸规则；主盘相关规则均为带前缀的 `.chart th, .chart td` / `.chart .rg td` / `.luck-table` 等
- 结论：`.xy-table` 在三端各出现 1 次且仅在星曜设置弹层（`<table class="xy-table">`，`index.html:976`）；主盘大运表 `div.luck-table`（`index.html:370`）**零影响**，`padding:1px 6px` 的生效范围仅设置页表格

---

## 7. 内测容器同步结果

`/Users/feng/.clacky/ext/local/bazi-paipan-test` 与真相源仓库逐文件 md5 **全部 SAME**：
`xingyao.js` `f07f04ec…` / `main.js` `1d437758…` / `index.html` `eb27d3a5…` / `standalone.html` `eb27d3a5…` / `standalone-split.html` `4cbcb3af…` / `scripts/check-release.sh` `a4241cb0…`（`sync-xingyao-algorithm.py`、`docs/ALGORITHM.md` 本阶段无变更，已 SAME）；容器内 `bash scripts/check-release.sh` **exit=0**。

---

*本记录双副本：本文件（真相源仓库 `docs/`）与工作目录 `实施记录_v0.30.0_星曜设置页迭代.md`，内容一致。*
