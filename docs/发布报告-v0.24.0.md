# 发布报告 v0.24.0（正式版 / GitHub Pages 线上）

> 发布师：worker_59b8f12a | 日期：2026-08-29 | 类型：正式版线上发布（T6）
> 前置：T5 内测发布完成（部署报告-v0.24.0.md）+ L4 用户手动验收通过

---

## 1. 版本号与发布方式

| 项 | 值 |
|----|----|
| 新版本 | **v0.24.0**（旧线上 v0.23.4，MINOR +1） |
| 发布方式 | git tag v0.24.0 + push main → GitHub Actions Pages build |
| 线上地址 | https://bangshun2025.github.io/bazi-paipan/standalone.html |
| 发布 commit | 78ae338（docs 部署报告）← 2eca5ac（v0.24.0 升版）← 07c7953（P0-01 修复）← 77037bf（v0.24.0 集成） |

## 2. 发布执行记录

1. `git tag v0.24.0`（本地，指向 78ae338）
2. `git push origin main`：01fa282 → 78ae338 ✅
3. `git push origin v0.24.0`：refs/tags/v0.24.0 ✅
4. GitHub Actions：回归测试 completed/success + pages build and deployment completed/success ✅

## 3. 线上验证结果（全部通过）

| 验证项 | 结果 |
|--------|------|
| 线上 standalone.html 版本标记 | ✅ 含 v0.24.0 |
| 线上 index.html / standalone.html 三入口 | ✅ 已更新 |
| config.js 线上可访问 | ✅ HTTP 200（647B，与本地一致） |
| auth.js 线上可访问 | ✅ HTTP 200（17473B） |
| records.js 线上可访问 | ✅ HTTP 200（23174B） |
| supabase.min.js 线上可访问 | ✅ HTTP 200（211907B） |

## 4. Supabase 账号体系线上可用确认

- **anonKey publishable 级别**：线上 config.js = `sb_publishable__OJl95ppP6lO7DsUCNdzBg_EF7Ru39J`（publishable 前缀，可公开）✅
- **RLS 行级安全生效**：
  - 档案记录：paipan_records 4 条 RLS 策略（select/insert/update/delete 均限 `user_id = auth.uid()`），GRANT authenticated 已补
  - 实测：匿名查询 `paipan_records` → HTTP 401 permission denied（anon 无法读表）✅
  - 历史实测：用户隔离通过（B 读不到 A 数据；伪造 user_id 插入被 RLS 拒绝 42501）✅
  - Supabase 为独立后端，不随 GitHub Pages 部署变化，线上即刻生效

## 5. R3 闭环验证（节气当天出生修复不丢失）

- v0.23.4 修复 commit `9fe5af3`（节气当天出生崩溃修复）+ `177834f`（同步遗漏补齐）均在 v0.24.0 祖先链 ✅
- 线上 standalone.html 含立春完整时刻 `lc2Ms`（2 处）✅
- 线上 standalone.html 含 `renYuanSiLing(ey, em, ed, eh, emi)` 5 参调用（4 处，不再退化为零点比较）✅
- 结论：节气当天出生排盘崩溃修复在 v0.24.0 正式版完整保留

## 6. 安全红线核查

- ✅ 选择性 git add（禁 git add -A），本次发布仅推送既有 commit，未混入未跟踪文件
- ✅ config.js anonKey publishable 可提交（PRD §9.2）
- ✅ 数据库密码（cetwmR4vxSVrJ0Pg）仅存档案 `/Users/feng/人生资产/10-开发项目/软件-八字排盘/八字排盘·档案/v0.24.0_Supabase账号功能/Supabase配置记录.md`（仓库外，未入库/未外发）
- ✅ .gitignore 防呆项生效（.bak*/、*密码*、Supabase配置记录*）
- ⚠️ 工作区遗留未跟踪：`docs/宫位数据排查报告.md`（编程师产出，与发布无关，未提交）

## 7. 待办建议

- **RELEASE 文档归档**：本报告已同步项目 docs/，后续由流程师统一收口 RETRO
- **宫位数据排查报告**：编程师产出的排查结论（v0.23.4→v0.24.0 宫位零改动，用户自定义数据因 localStorage origin 隔离未迁移）建议由流程师归档或通知用户处理
- **后续版本**：v0.24.1+ 可考虑宫位自定义数据云端化（records.js 云化只覆盖档案记录，宫位仍在 localStorage）
