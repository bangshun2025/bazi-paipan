# 部署报告 v0.25.0 — 正式发布（bazi-paipan · GitHub Pages）

> 发布师（worker_59b8f12a） · 2026-08-29
> 编排：orch_68e4b93af179（宫位配置保护迭代 T6 正式发布）

## 1. 版本号

- 旧版本：v0.24.0（公测线上）
- 新版本：v0.25.0（公测线上正式版）
- SemVer 判定：MINOR +1（向后兼容新功能：容错改造 + 导出导入 + 云同步）
- 用户已授权正式发布（Leader 第 17 轮指令）

## 2. 线上状态

| 项目 | 结果 |
|---|---|
| 线上 main | `330f999`（release commit）→ `c25cb4e`（ci 空 commit，触发 pages build） |
| 线上 tag | `v0.25.0`（annotated，message: v0.25.0: 宫位配置保护（容错+导出导入+云同步）） |
| pages build and deployment | ✅ completed success（head_sha=c25cb4e） |
| 回归测试 | ✅ completed success（330f999 / c25cb4e） |
| 线上入口 | https://bangshun2025.github.io/bazi-paipan/standalone.html → `<!-- 八字排盘 · 从真版 v0.25.0 | 2026-08-29 -->` ✅ |

## 3. CHANGELOG 条目（v0.25.0 — 宫位配置保护：容错 + 导出导入 + 云同步）

- **Added**：gongwei.js safeLoad/backupCorrupt/notifyCorrupt 容错；导出/导入配置（schema 版本化）；gongwei-cloud.js 云同步（AC10 云拉取/AC11 首传推云）；auth.js 登录/登出钩子；断言 247→269；check-release 增强（gongwei.js/gongwei-cloud.js 纳入比对）
- **Changed**：standalone.html/index.html/standalone-split.html 内联同步 + 导出导入 UI + 云同步 UI；api/handler.rb 新增 gongwei-cloud.js 路由；版本号 v0.24.0 → v0.25.0
- **Notes**：AC10/11 依赖 Supabase 建表 `docs/sql/user_gongwei_config.sql`；未建表时云同步静默降级，本地功能完全正常

## 4. 发布前校验（红线要求）

| 项目 | 结果 |
|---|---|
| check-release.sh 四步 | 🎉 全部校验通过（三文件语法 / index vs standalone 六模块一致 / 关键 id 全存在 / 外部 vs 内联六模块一致） |
| clacky ext verify | ✅ bazi-paipan api + launcher + paipan 全 [OK]（pre-commit 钩子自动复跑通过） |
| 三文件一致性（pre-commit） | ✅ 通过 |
| 选择性 git add | ✅ 16 文件（8 修改源码 + ext.yml/CHANGELOG/config.js/records.js 升版 + gongwei-cloud.js + 4 文档 + sql），无 `git add -A` |
| 敏感文件 | ✅ staged 无 password/secret/service_role/Supabase配置记录 |

## 5. 线上入口验证结果

| 资源 | HTTP | 说明 |
|---|---|---|
| /standalone.html | 200 | 头部注释 v0.25.0 |
| /gongwei.js | 200 | 容错模块（safeLoad/backupCorrupt/notifyCorrupt） |
| /gongwei-cloud.js | 200 | 云同步模块（syncConfig/cloud 函数在位） |
| 关键 UI id | 4/4 | btnExportConfig / btnImportConfig / gzFileImport / gzCloudSyncNote |

## 6. 建表状态（随正式版同步）

- **状态：待用户侧执行**（发布师无 Supabase 账号权限）
- SQL 文件：`docs/sql/user_gongwei_config.sql`（项目 vugckrqxqufiyfrpptwt 的 SQL Editor 执行，DDL → RLS 4 策略 → GRANT authenticated）
- 建表前：云同步按钮可用但静默降级（本地功能完全正常）；建表后：云拉取 / 首传推云完整生效
- 建议用户打开线上 /standalone → 登录 → 设置 → 宫位配置 → 云同步实测

## 7. 备注

- pages build 偶发不触发（330f999 push 后仅回归测试触发），按 bazi-publish-online 技能排查方法推空 commit `c25cb4e`（ci: 触发 GitHub Pages 重新部署）强制触发，pages build 成功
- check-release.sh 两处 bug 修复（版本号硬编码 → 正则；外部 vs 内联剥离不对称 → strip_header 对称化）已随 v0.25.0 入库
- 内测 bazi-paipan-test 保持 v0.25.0 本地 commit（8cceea8 + dcf0dbe），未 push（红线：内测 remote 与公测相同）
