# 回归测试报告 — initPresetArchives() 修复验证

**版本**: v0.9.0  
**测试日期**: 2026-07-21  
**测试师**: worker_2133bd85  
**测试方法**: Chrome DevTools Protocol (CDP) 直连浏览器自动化  
**被测文件**: `/Users/feng/clacky_workspace/新增名单到排盘软件/编程师/standalone.html`  
**变更点**: `initPresetArchives()` 从检测 `localStorage为空` 改为检测 `是否已有预置数据(isPreset)`  
**变更位置**: 第2762行 `function initPresetArchives()`

---

## 验证结果: ✅ 11/11 全部通过

### 场景1: 已有旧档案时16孩预置追加不重复
| 验证项 | 预期 | 实际 | 结果 |
|--------|------|------|------|
| 总数 | 18 (2旧+16预置) | 18 | ✅ |
| 预置档案数 | 16 | 16 | ✅ |
| 自建档案数 | 2 | 2 | ✅ |
| 邦顺保留 | 存在 | 存在 | ✅ |
| 测试儿保留 | 存在 | 存在 | ✅ |

**结论**: 旧档案+预置数据正确追加，互不覆盖，isPreset标记正确。

### 场景2: 预置数据不重复追加
| 验证项 | 预期 | 实际 | 结果 |
|--------|------|------|------|
| 总数 | 3 (2预置+1自建) | 3 | ✅ |
| 重复ID数 | 0 | 0 | ✅ |

**结论**: `hasPreset` 检测生效，已存在预置数据时不会重复追加。

### 场景3: 用户自建档案不被覆盖
| 验证项 | 预期 | 实际 | 结果 |
|--------|------|------|------|
| 邦顺生日 | 1990/3/15 保留 | `{"year":1990,"month":3,"day":15,"hour":8,"minute":30}` | ✅ |
| 总数 | 17 (1自建+16预置) | 17 | ✅ |

**结论**: 用户自建「邦顺」档案完整保留，预置数据追加不覆盖。

### 场景4: 浏览器实操功能回归
| 验证项 | 预期 | 实际 | 结果 |
|--------|------|------|------|
| 搜索过滤「子」 | 2个匹配 | 2 | ✅ |
| 清空恢复 | ≥16 | 17 | ✅ |
| 手风琴展开数 | 1 | 1 | ✅ |

**结论**: 搜索过滤、清空恢复、手风琴展开功能均正常。

---

## 测试方法说明

因 Chrome MCP bridge 连接异常，采用 CDP 直连方案：
- WebSocket 连接 Chrome DevTools (port 9224)
- 通过 `Runtime.evaluate` 直接在浏览器执行 JS 验证
- 所有JS代码以IIFE包裹，避免全局作用域 `return` 非法问题
- 绕过系统代理 (SOCKS5) 确保 WebSocket 连接正常

测试脚本: `/Users/feng/clacky_workspace/新增名单到排盘软件/测试师/cdp_verify.py`

---

## 结论

**✅ 修复有效，建议合入。** `initPresetArchives()` 的 `isPreset` 检测逻辑在三个核心场景下均按预期工作：旧档案追加、防重复、自建档案保护。浏览器实操功能无回归。
