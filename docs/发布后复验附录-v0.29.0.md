# 发布后复验附录 — v0.29.0 星曜功能

> 复验人：测试工程师（worker_9933551b）｜日期：2026-09-11｜性质：**只读独立复验**（未改任何源码/文档）
> 触发缘由：v0.29.0 内测发布打版本戳后，3 个前端产物 md5 相对 QA freeze 发生变化，按 Leader 闸门「产物变更即 QA 结论待复核」执行独立复算，**不采信发布师自证**。

## 一、复验对象指纹（当前已发布真实产物）

| 文件 | 当前 md5（源仓库） | 内测容器 | 发布前基线（`.bak_v0.29.pre/`） | vs QA freeze | 判定 |
|---|---|---|---|---|---|
| `index.html` | `26cc3e70f46412e8392487c6369871b8` | 同 | `6d1fe367cc894d9aa080764df515f40c` | CHANGED | 仅版本注释 |
| `standalone.html` | `26cc3e70f46412e8392487c6369871b8` | 同 | `6d1fe367cc894d9aa080764df515f40c` | CHANGED | 仅版本注释 |
| `standalone-split.html` | `d4640340ef0f8ad2835fcba827386ee9` | 同 | `682052a9a9ea4330a2c26fff2952ede3` | CHANGED | 仅版本注释 |
| `main.js` | `87539bce4181ea23ff9e79cf926eff7e` | 同 | `75f257851ef0922c995fb0044a41b3ab` | CHANGED | 仅版本注释 |
| `render.js` | `0afb05dc20b3898742d70a75207e545a` | 同 | 同（未变） | SAME | 未变 |
| `xingyao.js` | `a16caa34be449536f91f459a3d224f7d` | 同 | 同（未变） | SAME | 未变 |
| `docs/ALGORITHM.md` | `d52bc4f6f4dadfb60ad94f55f3e5364b` | 同 | —（QA 后未变） | SAME | 未变 |
| `CHANGELOG.md` | `07b5f45839bb041c4ccbc404a93a8db1` | 同 | `b8abd34534b8ef59a953ee52fc82df4b` | — | 文档追加 |
| `SYSTEM.md` | `7c1e611939c804588ac7781f63bc651c` | 同 | `6ff659af12f6f68ae32a6f6bd23c69cf` | — | 版本行 |
| `ext.yml` | `6684454cd463d91c60aa580864364392` | `8b495cc6…`（contributes 不同，预期） | — | — | 版本号 |

- `index.html == standalone.html`（`cmp` IDENTICAL，源/内测容器均成立）。
- 与 Leader/发布师通报的「发布后实际值」**逐条复核一致**（26cc3e70 / d4640340 / 87539bce）。

## 二、diff 判定（最关键）

**逐行 diff（`evidence/reverify/diff_all.txt`，对象 = `.bak_v0.29.pre/` 回滚基线）**

| 文件 | 增行 | 删行 | hunk | 变更内容 | 性质 |
|---|---|---|---|---|---|
| index.html | 3 | 2 | 2 | ①头部版本注释行 v0.28.0→v0.29.0 + 新增 v0.29.0 说明注释；②内联 main.js 首行 `/* … v0.28.0 — main.js */`→`v0.29.0` | **注释** |
| standalone.html | 3 | 2 | 2 | 同上 | **注释** |
| standalone-split.html | 2 | 1 | 1 | 头部注释块：版本行 + 新增 v0.29.0 说明注释 | **注释** |
| main.js | 1 | 1 | 1 | 第 1 行 `/* 八字排盘 v0.28.0 — main.js */`→`v0.29.0` | **注释** |
| render.js / xingyao.js | 0 | 0 | 0 | 完全一致 | — |
| CHANGELOG.md | 22 | 0 | 1 | 头部前置 v0.29.0 段落 | **文档** |
| SYSTEM.md | 2 | 1 | 2 | 版本行 + 版本表新增 v0.29.0 行 | **版本行** |
| ext.yml | 1 | 1 | 1 | `version: "0.28.0"`→`"0.29.0"` | **版本行** |

**独立强判据（不依赖 diff 的复核）**：`scripts/_commentless_check.py` 用状态机剥离 HTML 注释 / JS 行注释 / 块注释（尊重字符串字面量）后归一化比较——

```
index.html             去注释后 md5 = 1b22b9c4a03d77ef986e277d3c765dc3   == 基线  YES
standalone.html        去注释后 md5 = 1b22b9c4a03d77ef986e277d3c765dc3   == 基线  YES
standalone-split.html  去注释后 md5 = 92f80a9c234feb35ba9caebb7f83c917   == 基线  YES
main.js                去注释后 md5 = 548a1a27c3a7614c68bf344aab104fca   == 基线  YES
render.js              去注释后 md5 = 9c432b08758b474520cff642ebc3f6a6   == 基线  YES
xingyao.js             去注释后 md5 = 8aed36778978e0fc75584c1c3100145b   == 基线  YES
VERDICT: 去注释后全部一致 = YES（纯注释性变更）
```

> **去除注释后内容逐字节一致 ⇒ 变更 100% 属注释层，无任何逻辑改动。**
> **未发现任何非注释改动，未触发「发布事故」上报条件。**

## 三、执行命令与结果

| 复验项 | 命令 | 结果 |
|---|---|---|
| 发布一致性（源仓库） | `bash scripts/check-release.sh`（仓库根） | **exit=0，121 ✅ / 0 ❌** |
| 发布一致性（内测容器） | `bash scripts/check-release.sh`（`bazi-paipan-test/`） | **exit=0，121 ✅ / 0 ❌** |
| 末行（两容器一致） | — | `🎉 全部校验通过，可以发布。` |
| `?test=1` 三端 × file/http | `bash scripts/reverify_testsuite.sh` | **6 组全部 `total=517 / failed=0 / pass=true`**，标题「✅ 全部通过 — 八字排盘回归测试」 |
| 内测模块版入口 CDP 冒烟 | `python3 scripts/reverify_smoke_module.py`（`http://localhost:7070/api/ext/bazi-paipan-test/standalone`） | **8/8 全绿** |

**模块版入口冒烟明细（真实 HTTP 服务，非 file://）**：S0 入口 HTTP 200 / 69260B（模块版外壳，JS 外部加载）· S1 主容器+排盘成功、全局 `ALGO/RENDER/XINGYAO/APP/RECORDS/CONST` 均为 object · S2 盘面星曜行存在且**值格数与纳音行一致（6/6）** · S3 工具栏「星曜」按钮点击成功 · S4 设置页弹出（`shown=true`，表头 `六十甲子/纳音五行/星曜名称/来源体系`，首格 `甲子`）· S5 **60 数据行** · S6 交互过程**无新增 JS 异常**（errs=0）· S7 **无 `XINGYAO is not defined` 类报错**。

## 四、AC 结论

| 项 | 结论 |
|---|---|
| **是否发现非注释改动** | **否（逐行 diff + 去注释强判据双重确认）** |
| **QA 结论是否仍成立** | **是** —— AC01–AC14 全覆盖通过、TC-01~TC-32 32/32、**零缺陷**，结论**继续有效** |
| **是否构成发布事故** | **否**，未触发停线条件 |
| 遗留 | 已知限制 L1（`renderChartToHtml`/`renderExpandedChart` 未加星曜行、无调用点）不变，留 v0.29.x |

**独立判定：v0.29.0 内测发布产物与 QA 冻结版在逻辑上完全等价（仅注释/版本行/CHANGELOG/SYSTEM 版本行差异），发布后 QA 结论成立，准予放行。**

## 五、证据索引

`evidence/reverify/`：`diff_all.txt`（逐行 diff 原始输出）· `commentless_check.json`（去注释强判据）· `fingerprints.json` · `check_release_source.txt` / `check_release_test.txt` · `testsuite_{file,http}_{index,standalone,standalone-split}.json` + `testsuite_summary.json` · `cdp_smoke_module.json`。
复验脚本（只读）：`scripts/reverify_diff.sh`、`_commentless_check.py`、`reverify_testsuite.sh`、`reverify_smoke_module.py`、`_fingerprints_final.py`。
